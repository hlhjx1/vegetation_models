# 对比实验_三数据集_8模型训练.ipynb - 执行指南

## 📋 运行步骤与Cell对应表

### **必须运行的Cell**（按顺序）

| 序号 | Cell | 内容 | 说明 |
|------|------|------|------|
| 1 | **Cell 1** | 挂载Drive、安装依赖 | ⚠️ **首先必运** - 安装所有Python包 |
| 2 | **Cell 3** | 下载 LoveDA | 自动下载并预处理 LoveDA 数据集 |
| 3 | **Cell 4** | 下载 Potsdam + Vaihingen | Kaggle 数据集需要 kaggle.json 认证 |
| 4 | **Cell 5** | 预处理 LoveDA | 转换为植被二分类 patch（≤500张） |
| 5 | **Cell 6** | 预处理 Potsdam | 生成 512×512 patch（≤400张） |
| 6 | **Cell 7** | 预处理 Vaihingen | 生成 512×512 patch（≤400张） |
| 7 | **Cell 8** | 定义通用工具 | Dataset、损失、评估、训练函数 |
| 8 | **Cell 9** | 构建 DataLoader | 为三数据集 × 两尺寸(512/1024) 生成 DataLoader |

### **可选运行的Cell**（模型训练相关）

| 序号 | Cell | 内容 | 说明 |
|------|------|------|------|
| - | Cell 11-20 | 模型定义与逐数据集训练 | ✅ 可跳过 - 用已有权重 |
| - | Cell 22 | 绘制 IoU 柱状图 | ⚠️ 可选 - 需要前面训练结果 |

### **新增Cell**（交叉数据集可视化）

| 序号 | Cell | 内容 | 说明 |
|------|------|------|------|
| ✨ | **Cell 23** | 交叉数据集推理展示图 | **新添加** - 生成3份600DPI对比图 |

---

## 🎯 关键说明

### 1. **数据集重新下载** 

如果数据集已经在本地预处理过，可以跳过 Cell 3-7，直接从 Cell 8 开始。

```python
# 检查数据集是否已处理
import os
paths = [
    '/content/binary/loveda/JPEGImages',
    '/content/binary/potsdam/JPEGImages',
    '/content/binary/vaihingen/JPEGImages'
]
for p in paths:
    print(f"{p}: {len(os.listdir(p)) if os.path.exists(p) else 0} 张")
```

### 2. **Kaggle数据集认证**

Cell 4 中需要 `kaggle.json`：
- 从 https://www.kaggle.com/settings/account 下载
- 上传到 Colab 或本地路径
- 在 Colab 中：
  ```python
  !mkdir -p ~/.kaggle && !cp kaggle.json ~/.kaggle/
  !chmod 600 ~/.kaggle/kaggle.json
  ```

### 3. **模型权重来源**

已有的权重存储位置：
```
cross_dataset/
├── LoveDA/
│   ├── unet.pth
│   ├── deeplabv3plus.pth
│   ├── sam2tiny.pth
│   ├── mobilesam.pth
│   ├── sam21tiny.pth
│   └── mobilesamv2.pth
├── Potsdam/
│   └── (同上6个模型)
└── Vaihingen/
    └── (同上6个模型)
```

### 4. **新Cell 23 - 交叉数据集推理脚本**

将以下代码粘贴到 Notebook 最后的新 Cell 中：

```
[见下面粘贴的完整代码]
```

**功能**：
- ✅ 从三个数据集各抽取 **13 个样本**（总39个）
- ✅ 用已训练的 **6个模型** 进行推理（不含YOLO11和SegEarth-OV）
- ✅ 生成三份 **600DPI 高清对比图**：
  - `fig_loveda_6models_13cases_600dpi.png` (6886×3844px, ~47MB)
  - `fig_potsdam_6models_13cases_600dpi.png` (6886×3844px, ~47MB)
  - `fig_vaihingen_6models_13cases_600dpi.png` (6886×3844px, ~47MB)

**输出位置**：
```
/content/drive/MyDrive/vegetation_models_v2/paper_figures/
```

---

## 🚀 完整执行流程

### **情景A：从完全空白开始**

```
运行: Cell 1 → Cell 3 → Cell 4 → Cell 5 → Cell 6 → Cell 7 → Cell 8 → Cell 9 → Cell 23
预计时间：45-60 分钟
- 下载+预处理：25-30分钟
- 推理+可视化：15-20分钟
```

### **情景B：数据集已处理，要快速推理**

```
运行: Cell 1 → Cell 8 → Cell 9 → Cell 23
预计时间：  5-10 分钟
```

### **情景C：只想看图，模型权重已有**

```
运行: Cell 1 → Cell 23
预计时间：10-15 分钟
（但需要确保模型权重在 cross_dataset/ 中）
```

---

## 📊 产出示意

每份对比图包含：

```
行标签(a)-(g)    列标签 #1-#13
     ↓               ↓
(a)  原始图像   [13张RGB原图]
(b)  UNet        [13张预测覆盖]
(c)  DeepLabV3+  [13张预测覆盖]
(d)  SAM2_Tiny   [13张预测覆盖]
(e)  MobileSAM   [13张预测覆盖]
(f)  SAM2.1_Tiny [13张预测覆盖]
(g)  MobileSAMV2 [13张预测覆盖]

颜色含义：蓝色=TP | 绿色=FP | 粉色=FN
```

---

## ✅ 检查清单

在运行前检查：

- [ ] Google Drive 已挂载（/content/drive 可访问）
- [ ] Kaggle 认证文件准备好（如需下载Potsdam/Vaihingen）
- [ ] 可用磁盘空间 >10GB
- [ ] GPU 内存充足 (A100 或更高)
- [ ] 网络连接稳定

---

## 🔧 故障排查

### 问题1：Kaggle数据下载失败
**解决**：手动从Kaggle下载，上传到Colab：
```python
!unzip -q /content/potsdamvaihingen.zip -d /content/isprs
```

### 问题2：内存不足
**解决**：减少样本数或使用更小的batch size

### 问题3：推理超时
**解决**：逐个运行模型，减少并发加载

---

## 📝 最后提示

- **保存结果**：每次生成后记得Download图像
- **复用权重**：可将cross_dataset文件夹备份到本地
- **论文使用**：生成的图像已是出版质量 (600DPI)

