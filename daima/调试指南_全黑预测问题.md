# 🔍 全黑预测问题 - 调试指南

## 问题症状
- ✅ 脚本成功运行，没有错误
- ✅ 生成了7个输出文件
- ❌ 但所有6张预测图都是完全黑色（纯黑像素）
- 需要：代码修复 + 诊断步骤

---

## 🎯 立即可以做的事情（3选1）

### 选项1️⃣：快速测试 ⭐ 推荐
**时间：2分钟 | 难度：⭐ 简单**

在Colab中创建新Cell，复制粘贴：
```python
# 复制并运行 daima/zjs_quick_test.py 的全部内容
# 这会创建 /content/output_unet_test 目录
# 对比三种方法的预测效果
```

**然后看生成的三张图片：**
- `01_argmax.png` - 如果不全黑 → 当前方法有效✅
- `02_softmax_0.5.png` - 如果全黑 → 阈值太高❌
- `03_softmax_0.1.png` - 如果有内容 → 需要降低阈值🔧

**根据结果采取行动：**
```
情况A) 01或02有内容 → 直接运行修复版本
情况B) 只有03有内容 → 改阈值为0.1再试
情况C) 全部全黑 → 权重可能没加载，见下面"权重验证"
```

---

### 选项2️⃣：详细诊断
**时间：5分钟 | 难度：⭐⭐ 中等**

在Colab中创建新Cell，复制粘贴：
```python
# 复制并运行 daima/zjs_diagnosis.py 的全部内容
# 会看到类似输出：
#
# 📊 输出诊断信息:
#   输出类型: <class 'torch.Tensor'>
#   输出形状: torch.Size([1, 2, 512, 512])
#   输出值范围: [-15.234, 3.567]
#   通道0 (背景)...
#   通道1 (植被)...
```

**分析输出：**
- 如果 `通道1 (植被)` 的值都是负数或接近0 → 模型没学到
- 如果 `通道1 (植被)` 的值都是正数很大 → 需要 softmax 处理
- 如果 `方案2 (softmax>0.5)` 显示0% → 阈值太高

---

### 选项3️⃣：直接使用修复版本
**时间：1分钟 | 难度：⭐ 简单**

我已经改进了推理函数：

```python
# zjs_single_inference_final.py 的新 infer_model() 函数
# 改进点：
#
# 1. 自动处理多种输出格式（dict, tensor）
# 2. 自动处理多种张量形状（2D/3D/4D/5D）
# 3. 自动调整阈值（0.5 → 0.3 → 0.1 → 0.0）
# 4. 添加详细的debug输出
# 5. 更好的错误处理和追踪
```

**在Colab运行修复版本：**
```python
# 直接运行 zjs_single_inference_final.py
# 如果还是全黑，往下看"权重验证"
```

---

## ⚙️ 权重验证

如果三种方法都全黑，问题可能是权重加载。

检查清单：
```
[ ] 1. 权重文件是否存在？
      /content/drive/MyDrive/vegetation_models_v2/7_UNet/checkpoints/unet_best.pth
      
      在Colab运行：
      !ls -lh /content/drive/MyDrive/vegetation_models_v2/7_UNet/checkpoints/
      
[ ] 2. 权重id是否正确？
      在Colab运行：
      import torch
      ckpt = torch.load('/content/...unet_best.pth', map_location='cpu')
      print(list(ckpt.keys()))  # 应该看到 'model_state' 或 'state_dict'
      
[ ] 3. 模型初始化方式是否正确？
      查看相应的 load_xxx() 函数是否和checkpoint结构匹配
      
[ ] 4. 模型是否真的已经eval()？
      model.eval()
      with torch.no_grad():
          ...
```

---

## 📋 调试决策树

```
新推理函数运行后...
         ↓
    有错误吗？
    ├─ 是 → 看traceback（现在会打印详细的）
    └─ 否 ↓
      预测全黑吗？
      ├─ 是 → 运行 zjs_quick_test.py
      │        ├─ 01或02有内容? → 当前方法可用，继续用
      │        ├─ 只有03有内容? → 改阈值为0.1
      │        └─ 全黑? → 权重检查
      └─ 否 → ✅ 成功！
```

---

## 🔧 可能的手动调整

如果快速测试显示只有低阈值有效，编辑推理函数：

```python
# 在 zjs_single_inference_final.py 中找到：
for threshold in [0.5, 0.3, 0.1, 0.0]:
    preds = (veg_prob > threshold).squeeze(0).cpu().numpy().astype(np.uint8)
    if preds.sum() > 0:
        if debug and threshold != 0.5:
            print(f"    [DEBUG] 用阈值{threshold}获得{preds.sum()}个植被像素")
        break

# 改为：
for threshold in [0.1, 0.05, 0.0]:  # ⬅️ 从0.1开始，更激进
    preds = (veg_prob > threshold).squeeze(0).cpu().numpy().astype(np.uint8)
    if preds.sum() > 0:
        if debug and threshold != 0.1:
            print(f"    [DEBUG] 用阈值{threshold}获得{preds.sum()}个植被像素")
        break
```

---

## 📊 期望看到的输出

成功运行后，console应该显示：
```
✅ UNet推理完成 ... ✅ (植被覆盖率: 18.5%)
✅ DeepLabV3+推理完成 ... ✅ (植被覆盖率: 22.3%)
✅ SAM2_Tiny推理完成 ... ✅ (植被覆盖率: 15.7%)
✅ MobileSAM推理完成 ... ✅ (植被覆盖率: 19.2%)
✅ SAM2.1_Tiny推理完成 ... ✅ (植被覆盖率: 20.1%)
✅ MobileSAMV2推理完成 ... ✅ (植被覆盖率: 16.8%)
```

如果看到都是 `0.0%`，那就是阈值或权重问题。

---

## 📁 文件清单

已创建/修改的文件：

| 文件名 | 用途 | 运行方式 |
|--------|------|---------|
| `zjs_single_inference_final.py` | ✅ 修复版推理脚本 | 直接运行 |
| `zjs_single_inference_colab_cell.py` | 📋 Colab同步版本 | 复制到Colab |
| `zjs_quick_test.py` | 🔧 快速测试 | 在Colab Cell运行 |
| `zjs_diagnosis.py` | 🔍 详细诊断 | 在Colab Cell运行 |

---

## 🚀 下一步行动

**立即：**
1. 选择上面的3个选项之一
2. 按照指导运行
3. 查看输出或生成的图片

**如果成功：**
4. 运行完整版本（所有6个模型）
5. 检查 `output_zjs/` 中的7张图片
6. 如果满意 → 论文使用！

**如果失败：**
7. 查看这份指南中的"权重验证"部分
8. 或者在代码中添加 `debug=True` 查看详细信息

---

## 💬 调试技巧

**在Colab中添加debug模式查看中间结果：**

```python
# 在推理前设置
model.eval()

# 改为：
with torch.no_grad():
    output = model(img_tensor)
    print(f"原始输出形状: {output.shape}")
    print(f"输出最小值: {output.min()}")
    print(f"输出最大值: {output.max()}")
    print(f"第0通道平均值: {output[0,0].mean()}")
    print(f"第1通道平均值: {output[0,1].mean()}")
```

这样能直观看到每个通道的数值范围。

---

**最后一招：如果还是全黑，检查sigmoid vs softmax**

```python
# 尝试替代处理（某些模型用sigmoid而不是softmax）
import torch.nn.functional as F

# 用sigmoid代替softmax
probs = torch.sigmoid(output)  # ⬅️ 而不是softmax
preds = (probs[:, 1] > 0.5).squeeze(0).cpu().numpy()
```

---

## 📞 需要帮助?

提供以下信息能快速诊断：
1. zjs_quick_test.py 生成的三张预测图
2. zjs_diagnosis.py 的console输出
3. 修复版本运行后console的错误信息（如果有）
